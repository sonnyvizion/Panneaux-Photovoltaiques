{
  "name": "site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/deploy-headers.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "test": "vitest run",
    "images": "node scripts/optimize-images.mjs ../../../img/pages src/assets/pages",
    "deploy": "npm run build && npx wrangler@latest pages deploy dist --project-name=belgreen-demo"
  },
  "dependencies": {
    "@fontsource/geist-mono": "^5.2.8",
    "@fontsource/inter": "^5.3.0",
    "astro": "^7.0.6",
    "lenis": "^1.3.26"
  },
  "devDependencies": {
    "vitest": "^4.1.9"
  }
}
